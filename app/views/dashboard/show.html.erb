<%= link_to('Log out', destroy_user_session_path, method: :delete) %>

<h1>Your Tasks</h1>
<% if @tasks.any? %>
  <ul>
    <% @tasks.each do |task| %>
      <li><%= task.name %></li>
    <% end %>
  </ul>
<% else %>
  <p>You don't have any tasks!</p>
<% end %>

<%= link_to('Add Task', new_task_path, method: :get) %>
